"""FastAPI backend serving the SMP model-selection data to the Next.js frontend.

Run:
    uvicorn api.main:app --reload --port 8000

See `web/` for the Next.js frontend that consumes these endpoints.
"""
